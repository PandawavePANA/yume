# 인수인계

마지막 갱신: 2026-09-20(5) · 작성: Claude 세션 종료 시점
이 문서는 **공개 저장소에 올라갑니다.** 비밀값은 절대 여기에 적지 마세요. 어디에 있는지만 적습니다.

---

## 1. 지금 당장 해야 할 일

### 1-1. 포트원 결제·본인확인 — **코드는 끝났고 값만 넣으면 열립니다**

> **⏸ 2026-09-20 현재 막혀 있는 지점 — 다음 세션이 여기서 이어받으세요.**
>
> 결제창·본인확인·웹훅 코드는 전부 배포돼 있습니다(커밋 `d41acd3`·`2d86c9e`·`d437362`).
> **Railway `yume` 서비스에 포트원 값이 하나도 없습니다**(`railway.cmd variables -s yume --kv`로
> 확인했고 `CI_HASH_PEPPER` 하나만 들어가 있습니다 — 이건 제가 임의값으로 넣었습니다).
> 그래서 화면은 의도대로 예전 모습("이용 문의하기", "구매 신청")을 유지하고 있습니다.
> **사용자가 "아직 안 된다"고 하면 코드를 고치지 말고 이 변수부터 확인하세요.** 값 5개는
> 포트원 콘솔에서 사용자만 받을 수 있습니다(아래 표). 대신 넣어 줄 수 없고, 채팅으로 받아서도
> 안 됩니다(§8 작업 방식). 값이 들어오면 자동 재배포되고 코드 수정 없이 결제창이 열립니다.
>
> 값이 들어온 뒤 확인할 것:
> 1. 로그인 상태에서 `GET /api/checkout/config` → `payment`·`identity`가 null이 아니어야 합니다
> 2. 요금제 창에 "9,900원 결제하기"가 뜨는지 (안 뜨면 셋 중 하나가 빠진 것)
> 3. 웹훅은 포트원 콘솔의 "웹훅 테스트 발송"으로 200이 오는지 — 401/400이면 시크릿 불일치

결제창 호출과 서버 검증, 본인확인(KG이니시스 통합인증)까지 붙어 있습니다(2026-09-20).
**Railway 환경변수에 값을 넣는 순간 화면이 바뀝니다.** 값이 없으면 예전처럼 "구매 신청"만 받고
본인확인 단계도 나타나지 않으므로, 반쯤 설정된 상태로 두어도 사고가 나지 않습니다.

| 값 | 어디서 받나 | 환경변수 |
|---|---|---|
| Store ID (`store-...`) | 포트원 콘솔 → 결제 연동 → 연동 정보 | `PORTONE_STORE_ID` |
| 결제 채널 키 (`channel-key-...`) | 콘솔 → 채널 관리 → 채널 추가 → KG이니시스 | `PORTONE_CHANNEL_KEY` |
| 통합인증 채널 키 | 같은 화면에서 채널 하나 더(속성: 본인인증) | `PORTONE_IDENTITY_CHANNEL_KEY` |
| API Secret(서버 전용) | 콘솔 → 결제 연동 → API Secret | `PORTONE_V2_API_SECRET` |
| CI 해시용 비밀값 | **이미 넣어 뒀습니다(임의값)** | `CI_HASH_PEPPER` |
| 웹훅 시크릿 (`whsec_...`) | 콘솔 → 결제 연동 → 웹훅 → 추가 | `PORTONE_WEBHOOK_SECRET` |

웹훅 주소는 `https://www.yume-reamer.com/api/portone/webhook` 입니다. 콘솔에 이 주소를 등록하고
받은 시크릿을 환경변수에 넣으세요. **시크릿이 없으면 웹훅은 503으로 거절합니다**(검증할 수 없는
요청으로 크레딧을 주지 않기 위해서입니다).

**API Secret에는 절대 `VITE_` 접두사를 붙이지 마세요.** 붙이는 순간 빌드 결과물에 박혀 누구나 봅니다.
Store ID와 채널 키는 브라우저에 나가도 되는 공개값이라 서버가 화면에 내려 줍니다.

#### 포트원 콘솔에서 채널 만드는 법 (심사용 테스트 모드)

포트원이 "테스트모드를 설정해야 PG 심사가 진행된다"고 안내한 그 설정입니다.
콘솔 → 결제 연동 → 연동 정보 → 채널 관리 → **+ 채널 추가**.

1. **결제 채널** — 연동 모드 `테스트 연동` · 결제대행사 `KG이니시스` ·
   결제모듈 `결제창 일반/정기결제 및 API 수기/정기결제` · 채널 속성 `결제` ·
   PG상점아이디 `INIpayTest`
2. **본인인증 채널** — 연동 모드 `테스트 연동` · 결제대행사 `KG이니시스` ·
   결제모듈 `본인인증 통합인증` · 채널 속성 `본인인증` · PG상점아이디 `MIIiasTest` ·
   과세설정 **미설정**

계약이 끝나면 같은 자리에서 `실 연동` 채널을 만들고(상점아이디 MOI…/MII…, 웹표준결제 signkey,
INIAPI key) 환경변수의 채널 키만 바꾸면 됩니다. 코드는 그대로입니다.

> **테스트 모드 주의** — 이니시스 테스트 결제는 **실제로 출금됐다가 매일 23:00~23:50에 자동 취소**됩니다.
> 국민카드 계열(카카오뱅크 포함)은 테스트 결제가 되지 않습니다.
> 카카오 인증서로 CI를 받으려면 이니시스와 **별도 서류**가 필요합니다(통합인증 계약 담당자에게 요청).

#### 코드가 지키고 있는 것 — 되돌리지 마세요

- 크레딧은 **서버가 포트원에 직접 조회해 금액이 맞을 때만** 들어갑니다(`server/portone.js`).
  브라우저가 "결제됐다"고 보낸 값은 쓰지 않습니다
- 같은 결제를 두 번 확인해도 한 번만 지급됩니다(`credit_orders.status`를 조건으로 갱신)
- 가상계좌는 입금 전이라 지급하지 않습니다
- **CI는 저장하지 않습니다.** `CI_HASH_PEPPER`를 섞은 해시만 남깁니다(같은 사람의 중복 가입 확인용).
  방침에 그렇게 적혀 있으니 CI 원문을 저장하는 코드를 넣지 마세요
- 본인확인은 가입 때 CI 동의를 받은 계정만 시작할 수 있습니다

`server/tests/checkout.test.js`가 위 내용을 전부 잡고 있습니다(금액 위조·중복 확인·남의 주문·
가상계좌·CI 미저장). 결제 코드를 고치면 이 테스트부터 돌리세요.

#### 파는 것 두 가지

| 상품 | 주문 | 결제가 확인되면 |
|---|---|---|
| 크레딧 팩 (12,000 / 33,000 / 100,000원) | `POST /api/checkout { packKey }` | 크레딧 적립 |
| 요금제 1개월 이용권 (스탠다드 9,900 · 전문가 29,000원) | `POST /api/checkout { plan }` | `users.plan`과 `plan_expires_at` 30일 연장 |

요금제는 **자동 갱신이 아닙니다.** 1개월 이용권을 파는 구조라 빌링키를 저장하지 않습니다
(저장하면 정기결제가 되어 심사 항목과 환불 조건이 달라집니다). 남은 기간이 있으면 그 뒤에
붙이고, 결제가 취소되면 그 30일을 도로 깎습니다.

무료·비즈니스는 `monthlyKrw`가 없어 주문 자체가 거절됩니다 — 파는 상품이 아니라 문의 상품입니다.
**화면 가격(`src/YumeDashboard.jsx`의 `PLAN_PRICE_KRW`)과 서버 가격(`server/plans.js`의
`monthlyKrw`)은 같아야 합니다.** 청구는 서버 값으로 되므로, 어긋나면 화면과 청구가 달라집니다.

#### 웹훅 (2026-09-20 추가)

`server/portoneWebhook.js`. 가상계좌 입금 완료와 결제 취소·환불을 포트원이 알려 줍니다.

- **서명을 반드시 확인합니다.** 공개 주소라 확인하지 않으면 누구나 "결제됐다"고 보내 크레딧을
  받아 갑니다. 포트원 공식 SDK(`@portone/server-sdk`)가 Standard Webhooks 규격으로 검증합니다
- 서명은 **본문 원문**에 대해 계산되므로 이 경로만 JSON 파서보다 앞에 붙어 있습니다.
  `app.js`에서 순서를 바꾸면 검증이 조용히 깨집니다
- 지급은 화면 확인과 웹훅이 `server/checkoutStore.js`의 같은 함수를 쓰므로, 두 길로 같은 결제가
  들어와도 한 번만 들어갑니다
- 취소가 오면 지급했던 크레딧을 원장에서 되돌립니다. 이미 써 버렸으면 **잔액이 음수가 됩니다** —
  쓴 만큼은 실제로 나간 비용이라 없던 일로 만들 수 없고, 음수를 감추면 장부가 사실과 달라집니다

#### 아직 남은 것

- 정기결제(구독)는 붙이지 않았습니다. 요금제는 여전히 운영자가 직접 바꿉니다
- 잔액이 음수가 된 회원을 운영자가 볼 화면은 없습니다(관리자 → 크레딧에서 원장으로만 확인)

### 1-2. API Secret 재발급

채팅으로 주고받은 값이라 대화 기록에 남아 있습니다. 연동을 마친 뒤 포트원 콘솔에서
한 번 재발급하시고 `.env`만 바꾸세요. 코드는 손댈 필요 없습니다.

### 1-3. 통신판매업 신고

카드사 심사 필수 6항목에 들어 있는데 아직 없습니다. 정부24 또는 관할 구청에서 신고하고
번호를 받으면 환경변수 두 개로 전 화면에 반영됩니다.

```
MAIL_ORDER_NO=제2026-대구달성-0000호
VITE_MAIL_ORDER_NO=제2026-대구달성-0000호
```

번호가 없으면 화면에 그 줄이 아예 안 나옵니다 — 없는 번호를 적는 것보다 낫기 때문입니다.

### 1-4. 심사용 계정

크레딧 구매가 로그인 뒤에 있어서, 카드사에 ID/PW를 전달해야 합니다.
계정 하나 만들어 크레딧을 넉넉히 넣어두고 회신하세요.

### 1-5. apex 도메인 DNS

`yume-reamer.com`(www 없음)이 아직 호스팅어 파킹 페이지를 가리킵니다.
서버에 apex→www 301 리다이렉트는 넣어 뒀으므로, DNS만 Railway로 돌리면 바로 작동합니다.
돌리지 않을 거면 **어디에 적든 반드시 `www`를 붙이세요.**

---

## 2. 사업자 정보

사업자등록증 기준이며 코드에 기본값으로 박혀 있습니다.

| 항목 | 값 |
|---|---|
| 상호 | 리머 (REAMER) |
| 대표자 | 정원영 |
| 사업자등록번호 | 627-03-03900 |
| 사업장 소재지 | 대구광역시 달성군 유가읍 테크노대로5길 80, 212동 1402호 (호반베르디움 2차) |
| 대표전화 | 053-557-3415 (유선번호 — 심사가 휴대폰을 인정하지 않음) |
| 이메일 | reamer@d-reamer.com |
| 개업일 | 2026-09-16 · 간이과세자 |
| 통신판매업 신고 | **미신고** (1-3 참고) |

**주민등록번호는 어디에도 넣지 않았습니다.** 등록증에는 있지만 공개 항목이 아니고
이 저장소는 public입니다. 앞으로도 넣지 마세요.

값이 있는 곳 — 두 곳이 짝입니다. 고칠 때 둘 다 봐야 합니다.

- 프런트엔드: `src/businessInfo.js`
- 서버 렌더 페이지: `server/renderPages.js`의 `COMPANY`

환경변수로 덮어쓸 수 있지만 **기본값을 비우지 마세요.** 환경변수를 빠뜨린 배포에서
항목이 조용히 사라지고, 실제로 그것 때문에 심사가 한 번 반려됐습니다.

---

## 3. 요금제와 원가 구조

검증 1건 실측 원가는 **0.05~0.23달러, 중앙값 약 0.15달러(환율 1,400원이면 약 210원)** 입니다.
`server/apiCost.js`가 건마다 토큰·검색·캐시 적중을 기록합니다.

**모든 가격은 이 원가에서 거꾸로 잡았습니다.** 요금제를 전부 소진해도 원가율이 55%를 넘지 않습니다.

| 상품 | 가격 | 지급 | 건당 단가 |
|---|---|---|---|
| 무료 | 0원 | 월 10 크레딧 | — |
| 스탠다드 | 9,900원/월 | 월 25 크레딧 | 396원 |
| 전문가 | 29,000원/월 | 월 75 크레딧 | 387원 |
| 크레딧 20개 | 12,000원 | — | 600원 |
| 크레딧 60개 | 33,000원 | — | 550원 |
| 크레딧 200개 | 100,000원 | — | 500원 |

추가 구매가 구독보다 건당 단가가 **비싼 것은 의도한 설계**입니다. 예산이 미리 정해지는
구독 쪽이 서로 이득이 되도록 한 것이니 뒤집지 마세요.

크레딧은 **입력 2,000자마다 1개**씩 차감됩니다(`CHARS_PER_CREDIT`). 길이와 무관하게
1건=1크레딧으로 되돌리면 긴 문서를 넣는 사람이 짧게 쓰는 사람의 보조를 받게 됩니다.

### 절대 하면 안 되는 것

- 결제 없이 유료 플랜이나 크레딧 지급
- 크레딧을 현금으로 환전
- 하루 한도를 월 지급량보다 크게 설정 — 한도가 걸리기 전에 원가가 먼저 터집니다

---

## 4. 기여도 시스템 (크레딧과 다른 값)

크레딧은 쓰면 없어지는 소비재, 기여도는 쌓이는 점수입니다. **원장과 테이블이 따로입니다.**
섞으면 검증할 때마다 순위가 내려갑니다.

| 행동 | 점수 |
|---|---|
| 검증 | 10점 |
| 초대 (친구의 첫 검증 완료 시) | 30점 |
| 발견 (사실과 다른 주장이 잡힘) | 50점 |
| 제보 (운영자 검토 통과) | 1,000점 |

**순서가 설계입니다.** 초대가 발견보다 커지면 랭킹이 "많이 찾아낸 사람"이 아니라
"많이 데려온 사람"의 보드가 됩니다. 이 순서를 고정하는 테스트가 있습니다.

분기마다 초기화되고 1~3위 골드바, 4~10위 크레딧입니다.
골드바는 분기당 시세 기준 약 86만원(월 환산 29만원)이 나가니, 초기 이용자가 적을 때는
**분기 활성 이용자 수 조건을 거는 걸 검토**하세요.

파밍 방지 — 전부 "제출"이 아니라 "결과"에 보상을 걸었습니다. 풀지 마세요.
- 초대: 친구가 실제 검증을 마쳐야 지급, 월 20건 상한, 같은 IP는 검토 보류
- 제보: 운영자 승인 시에만 지급, 중복 인용은 보상 없음
- 발견: 여러 건이 잡혀도 검증당 1회

---

## 5. 심사 관련 페이지

전부 서버 렌더링이고 로그인 없이 열립니다 (`server/renderPages.js`).

| 경로 | 용도 |
|---|---|
| `/terms` | 이용약관 |
| `/privacy` | 개인정보처리방침 |
| `/refund` | 환불정책 — 전자상거래법 제17조 기준 |
| `/products` | 상품 안내 — 카드사가 로그인 없이 상품·가격 확인 |
| `/account-deletion` | 앱스토어 심사 필수 |
| `/docs/api` | 검증 API 문서 |

**환불정책 내용을 한 번 읽어보세요.** 공제 단가 계산(결제액 ÷ 지급 크레딧 수)과
처리 기한(접수 3영업일, 취소 3영업일)은 일반 기준으로 정한 값입니다.
심사를 통과하면 실제로 지켜야 하는 약속이 됩니다.

사업자 정보는 푸터뿐 아니라 **결제가 일어나는 화면마다** 들어갑니다
(`src/components/yume/BusinessInfo.jsx`). 요금제 모달, 크레딧 탭, 사이드 메뉴에 있습니다.
모달이 푸터를 덮기 때문에 푸터에만 두면 결제하는 순간에는 화면에 없습니다.

---

## 6. 제품 핵심

### 부존재 신뢰도 (NEC) — 특허 출원 준비 중

`NEC = w₁·C + w₂·F + w₃·(1−P)` · 임계값 0.7

- C 탐색 커버리지 · F 형식 오류 · P 근접도
- 가중치: 법률 `{0.65, 0.2, 0.15}` · 학술 `{0.6, 0.25, 0.15}` · 일반 주장 `{0.7, 0.1, 0.2}`
- 코드: `server/nec/`

**특허 출원 전까지 명세서 초안은 절대 커밋하지 마세요.** `.gitignore`에 규칙이 있습니다.
공개되면 신규성이 깨집니다.

### 정확도 보호 장치

- **정확도 회귀 시험** `npm run test:accuracy` — 정답이 확정된 8건. 원가를 줄이는 변경이
  이걸 깨뜨리면 **아낀 돈과 무관하게 되돌리세요.** 정확도가 곧 제품입니다.
- **출처 없는 판정 차단** (`server/claimGuard.js`) — 출처 0건이면 confirmed/false로 못 나갑니다
- **지목 재확인** (`server/reviewAccusations.js`) — "사실과 다름"만 다시 봅니다.
  맞는 정보를 거짓이라 하는 게 가장 해로운 오류입니다.

### 법제처 연동

- 조문 원문을 받아 글자 단위 대조, **오늘 시행 중인 버전만**
- 고시·훈령은 발령번호가 색인되지 않아 부처별 목록을 훑습니다. 부처 코드 27곳은
  전부 API 응답에서 확인하고 역조회로 검증한 값입니다 — **추정해서 추가하지 마세요**
- 법률 캐시는 한국 날짜로 키를 겁니다. 자정에 통째로 무효가 돼야
  개정 전 조문으로 "맞다"고 하는 일이 없습니다

---

## 7. 개발 환경

```bash
npm run dev            # 서버 + 클라이언트
npm test               # 138개 (전부 통과 상태를 유지할 것)
npm run test:accuracy  # 정확도 회귀 — API 키 필요, 실제 비용 발생
npm run build          # 유메 → dist
npm run build:business # 기업용 → dist-business
npm run build:reamer   # 회사 소개 → dist-reamer
```

빌드가 셋인 이유는 도메인이 다르기 때문입니다.

### 환경변수

비밀값은 로컬 `.env`(gitignore됨)와 Railway에 있습니다. 이름만 적습니다.

| 이름 | 용도 |
|---|---|
| `ANTHROPIC_API_KEY` | 검증 엔진 |
| `LAW_OC` | 법제처 API |
| `DATABASE_URL` | 운영 Postgres. 없으면 로컬 PGlite |
| `PUBLIC_BASE_URL` | `https://www.yume-reamer.com` — 메일 링크에 쓰임 |
| `PORTONE_V2_API_SECRET` | 포트원 API 시크릿. **`VITE_` 붙이지 말 것** |
| `PORTONE_STORE_ID` / `PORTONE_CHANNEL_KEY` | 결제창(없으면 구매 신청만 받음) |
| `PORTONE_IDENTITY_CHANNEL_KEY` | 본인확인 통합인증(없으면 본인확인 단계가 안 보임) |
| `CI_HASH_PEPPER` | CI 해시에 섞는 값. 바꾸면 기존 해시와 대조가 끊김 |
| `BUSINESS_TEL` / `VITE_BUSINESS_TEL` | 유선번호 |
| `MAIL_ORDER_NO` / `VITE_MAIL_ORDER_NO` | 통신판매업 신고번호 |
| `ADMIN_EMAILS` / `ADMIN_SECRET` | 관리자 |
| `AUDIT_ALLOWED_ORIGINS` | 기업용 사이트에서 무료 진단 API 호출 허용 |

---

## 8. 작업 방식

- **비밀값은 채팅으로 주고받지 않습니다.** API 키·시크릿은 사용자가 Railway 대시보드나
  로컬 `.env`에 직접 넣습니다. 대화로 받으면 대화 기록 파일에 남고, 그러면 그 값은
  재발급해야 합니다(포트원 API Secret이 실제로 그렇게 됐습니다). 값이 필요하면
  **어느 변수에 무엇을 넣어야 하는지만 안내**하세요
- **커밋·푸시는 요청받았을 때만.** 저장소가 public이니 커밋 전 비밀값을 한 번 훑으세요
- `git add -A`를 조심하세요. 32MB 영상이 한 번 딸려 들어간 적이 있습니다
  (지금은 `*.mp4`가 gitignore에 있습니다)
- 원가를 줄이는 변경은 **반드시 정확도 회귀와 함께** 판단하세요
- 유메는 "AI 서비스"가 아니라 **팩트체크 서비스**로 소개합니다
- 데이터셋에는 동의받은 건만 넣습니다

---

## 9. 발표 자료

서사 구조 25장, 실제 화면 캡처 6장 포함. 스페이스바로 넘기고 대본이 아래에 붙습니다.

https://claude.ai/artifact/Q8XgJRbT34UxNm2d55Ei2Q

흐름: 왜 만들었나 → 문제 → 부존재 신뢰도 → 신뢰 장치 → 초기 유치(크레딧·초대·랭킹·제보)
→ 핵심 자산(팔 데이터) → 수익 → 기업 → **식품 청결 마크 비유 → 팩트체크 인증 마크** → 로드맵

마지막 세 장이 포부 파트입니다. 식품 회사가 스스로 붙인 청결 마크를 아무도 믿지 않듯,
AI 업계도 자기 검증만 하고 있다 → 유메가 그 시대의 인증 마크가 된다는 구조입니다.

---

## 10. 이번 세션에서 한 일

```
6c19fe0  공개 상품 안내 페이지 (/products)
3014bda  32MB 영상 추적 해제
955638e  유선번호 053-557-3415 추가
1e29358  PG 심사 항목 구축 (환불정책, 결제 화면 사업자정보)
0c01cfd  붙여넣기 버튼
485661e  apex → www 리다이렉트, 메일 링크 기본값
fd4ed55  제보 보상 문구 정정 (크레딧 → 기여도)
2dedd76  초대에 기여도 30점 지급
bf7eef7  요금제를 실측 원가에서 역산
3a20452  모든 공개 페이지에 사업자 정보
```

가장 중요한 건 `bf7eef7`입니다. 그전에는 스탠다드 9,000원에 월 3,000 크레딧이었습니다.
전부 쓰면 **9,000원 받고 63만원을 쓰는 구조**였고, 추가 구매 팩도 원가 이하라
팔릴수록 손해였습니다. 산술을 한 번도 안 해본 값이었습니다. 다시 그렇게 만들지 마세요.

---

## 11. 2026-09-18~20 세션에서 한 일

```
1077128  가입 화면에 본인확인(CI) 동의
304a50f  개인정보처리방침에 CI 수집 항목 추가
94c8859  감사 채점이 실재 판례를 "지어냈다"고 하던 문제 수정
d57ff3f  문항별로 AI가 한 말과 공식 확인 결과를 나란히 표시
5b126fc  클립보드 API가 막힌 브라우저에서도 복사되게
2a90cda  한도를 새어 나가게 하던 경쟁 상태·누락 수정
```

### 기업용 사이트가 배포된 적이 없었습니다

상단 "기업용" 버튼이 `business.yume-reamer.com`을 가리키는데 그 주소가 DNS에 없었습니다.
Railway `yume` 프로젝트에 **`business` 서비스를 새로 만들어** 배포했습니다(리머 서비스와 같은 방식).

- 빌드 `npm run build:business` · 실행 `npx serve dist-business -s -l $PORT`
- 호스팅어 DNS에 CNAME `business` → `15z5zoyp.up.railway.app`, 소유 확인용 TXT 추가(완료)
- `AUDIT_ALLOWED_ORIGINS=https://business.yume-reamer.com` 를 yume 서비스에 넣어야 기업용
  페이지의 무료 진단이 동작합니다. **넣어 뒀습니다.** 도메인이 바뀌면 이 값도 같이 바꾸세요

### 채점이 실재하는 판례를 부존재로 판정하던 문제 (가장 중요)

실제 감사에서 **대법원 2017. 5. 17. 선고 2014다232296, 232302**를 "존재하지 않는 근거"로
찍었습니다. 실재하는 병합 사건입니다. 법제처 판례 DB는 병합 사건을 사건번호 칸 하나에
`"2014다232296, 232302"`로 넣는데, 조회 코드가 그 칸 전체와 문자열을 비교해서 두 번호 다
못 찾았습니다. **같은 조회를 일반 검증도 씁니다** — 감사만의 문제가 아니었습니다.

- `expandCaseNumbers()`로 병합 번호를 펼칩니다(뒤 번호는 앞 번호의 연도·부호를 이어받음)
- 사건번호 검색이 비면 본문 검색으로 후보를 받아 **본문 조회의 사건번호 칸**으로 확인합니다
- 인용 조회 결과를 **실재 / 존재하지 않음 / 조회 불가** 셋으로 나눴습니다.
  조회 실패를 "실재"로 표시하던 게 더 위험했습니다 — 없는 근거를 있다고 말하게 됩니다

### 채점 자체를 못 믿게 만드는 것들도 같이 고쳤습니다

- 채점 모델을 하이쿠 → **소네트**로 올리고, **두 번 따로 채점해 일치할 때만** 받아들입니다.
  갈리면 한 번 더, 그래도 다수가 없으면 **판정 보류**(억지로 실패를 매기지 않음)
- 채점자가 인용한 근거 문장이 **답변에 실제로 없으면 그 표를 버립니다**(채점자의 할루시네이션)
- 유형별 판정 기준을 명시 — "없다고 정확히 답했는데 곁다리 사실을 틀린 것"은 할루시네이션이
  아니라 부분 실패, 다른 조문이라고 밝히고 덧붙인 설명과 조문 요소를 정리한 것은 감점 대상 아님
- 제안 문구에 내부 코드(`fabrication_bait`)가 그대로 나가던 것 → 한글 유형명

실측 9건으로 확인했고 8건이 기대와 일치했습니다(1건은 근로기준법 60조 연차 일수를 틀린 답을
부분 실패로 봤는데 채점은 할루시네이션으로 판정 — 핵심 수치라 그쪽이 맞다고 보고 두었습니다).

### 본인확인(CI) 동의

통합인증서비스 신청서가 **개인정보처리방침 수집 항목에 "CI"를 요구**합니다. 없으면 카카오
인증서만 열립니다. 방침(`server/renderPages.js`)과 가입 화면(`AuthModal.jsx`) 양쪽에 넣었고,
`agreeIdentity` 없이는 가입이 400으로 막힙니다. 동의 시각은 `users.identity_agreed_at`.

- **CI 값 자체는 저장하지 않습니다.** 실제 인증 연동(창 호출·CI 수신)은 아직 없습니다
- 방침에 위탁 업체를 **주식회사 케이지이니시스**로 적었습니다. 계약이 다른 곳으로 정해지면
  그 줄을 먼저 고치세요
- 방침은 이제 약관과 **다른 시행일**을 씁니다(`PRIVACY_EFFECTIVE_DATE`). 고칠 때 개정 이력도 같이
- 밸러스트(`C:Usersjwy33amm-protocol-site`)도 같은 날 같은 방식으로 맞췄습니다

### 그 밖의 버그

- 복사 버튼이 카카오톡 인앱 브라우저 등에서 먹히지 않던 문제 — `src/clipboard.js`로 모아서
  `execCommand` 폴백 추가(문항 복사·API 키·추천 링크·결과 공유 네 곳)
- 결제 크레딧 지급을 두 번 누르면 두 번 처리될 수 있던 것, 재설정 링크 동시 사용, `/v1` 월 한도를
  대기 시간 동안 넘길 수 있던 것, 검증 실패 시 IP 사용량이 안 돌아오던 것
- 검증 후속 작업(기록 정리·기여도)이 실패하면 크레딧을 환급하고 오류를 보내던 것 — 결과는 이미
  저장돼 있어 공짜 검증이 됐습니다
- 스키마 접두어 목록에 빠져 있던 테이블 7개(credit_ledger 등) 추가

### 결제·본인확인 연동 (2026-09-20)

포트원 메일이 "테스트모드를 설정해야 PG·카드사 심사가 진행된다"고 해서, 유메에 없던
결제창 호출을 붙이고 본인확인까지 함께 넣었습니다. 자세한 설정법은 §1-1에 있습니다.

- `server/portone.js` — 결제 조회·금액 대조, 본인확인 조회, CI 해시
- `server/creditsApi.js` — `/api/checkout`(주문 생성) · `/api/checkout/confirm`(지급) ·
  `/api/identity/start` · `/api/identity/confirm` · `/api/checkout/config`(화면이 켤지 말지 판단)
- `src/payments.js` — 결제창·인증창 호출과 모바일 리다이렉트 복귀 처리
- `credit_orders` 테이블 추가, `users`에 `identity_verified_at`·`identity_ci_hash`·`identity_name`
- `server/portoneWebhook.js` · `server/checkoutStore.js` — 웹훅과 정산(지급·취소 되돌리기)
- 밸러스트에도 같은 방식으로 본인확인을 붙였습니다(결제는 이미 있었음)
- 동의 항목이 생기기 전에 가입한 회원은 `identity_agreed_at`이 비어 있어 본인확인을 시작조차 할 수
  없었습니다. 인증창을 여는 자리에서 같은 동의를 받고 시각을 남기도록 고쳤습니다(양쪽 사이트 모두)
