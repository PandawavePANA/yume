node_modules/
dist/
dist-reamer/
dist-business/
.env
.claude/
.originkit/
data/
*.db
*.db-wal
*.db-shm
.env.railway

# 출원 전 특허 명세서 — 이 저장소는 공개라 커밋하면 그 자체가 공지(공개 실시)가 되어
# 신규성이 깨질 수 있다. 출원을 마친 뒤에 올릴 것.
특허명세서_*.docx
시점인식기반AI사실검증_*.docx

# 앱 서명 키·비밀번호(절대 커밋 금지)
android-signing/
android/keystore.properties
*.jks
*.keystore
*.p12
*.p8
*.mobileprovision

# 영상·녹화 파일. 발표용으로 폴더에 두고 쓰는 것이라 저장소에 올릴 이유가 없다.
# (유메.mp4가 32MB로 한 번 올라간 적이 있다 — public 저장소라 더 그렇다.)
*.mp4
*.mov
*.webm
