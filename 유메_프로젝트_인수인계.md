# 유메(YUME) 프로젝트 인수인계 문서

이 문서는 Claude(claude.ai)와의 긴 대화에서 결정된 내용을 요약한 것입니다.
작업을 시작하기 전에 이 문서를 먼저 읽어주세요.

## 서비스 한 줄 정의
**할루시네이션(AI가 틀린 정보를 자신 있게 말하는 현상) 방지 팩트체크 서비스.**
ChatGPT·클로드·제미나이 등 AI의 답변을 붙여넣으면, 그 안의 사실 주장을 추출해서
검증하고 신뢰도를 표시해준다.

⚠️ 오해하기 쉬운 지점 — 반드시 정확히 이해할 것:
**"공공데이터를 대조하는 도구"가 정체성이 아니다.** 공공데이터 대조는 검증에 쓰는
여러 수단 중 하나일 뿐이다. 서비스의 본질은 "AI 답변이 틀렸을 때 그걸 잡아내는 것"이고,
그 방법이 도메인에 따라 다를 뿐이다(일반 주제는 웹검색, 법률/의료/금융처럼 규제·전문성이
필요한 영역은 공식 DB 대조 + 필요 시 향후 전문가 검수 연계까지 포함 가능).

## 수익모델 — 왜 이게 중요한 설계 축인가
- **무료 티어**: 일반적인 주제의 AI 답변 검증 (가벼운 웹검색 기반)
- **유료 티어**: 법률·의료·금융처럼 "틀리면 실제 피해가 나는" 전문 분야를 깊게 검증
  - 여기가 핵심 수익 포인트다. "돈을 내면 전문 분야까지 확실하게 확인해준다"는
    느낌을 프로덕트 설계·UI(락 아이콘, 업그레이드 유도 등)에 명확히 드러내야 한다.
  - 무료와 유료의 차이가 사용자에게 시각적으로 분명히 보여야 함(예: 무료 사용자가
    법률 도메인 질문을 검증하려 하면 "전문 분야 검증은 Pro에서" 같은 업셀 지점 필요)

## 핵심 포지셔닝 (매우 중요 — 절대 잊지 말 것)
**유메는 "AI 서비스"가 아니라 "팩트체크 서비스"로 포지셔닝한다.**
신용평가사가 스스로를 알고리즘 회사라 부르지 않듯, 유메가 파는 것은 AI 기술이 아니라
"확인된 사실"이다. 이후 모든 UI 카피, 기능 설명은 "AI가 확인했다"가 아니라
"유메가 확인했다"는 어조를 유지해야 한다. 마찬가지로 "공공데이터 대조 툴"이라는
인상도 주면 안 된다 — 핵심은 할루시네이션 방지이고, 공공데이터는 그 수단 중 하나다.

## 왜 이 사업인가 (배경)
- 창업자 본인이 법정 분쟁 중 AI(클로드)가 제공한 잘못된 정보로 실제 피해를 입은 경험이 발단
- 법정 문서에서 AI 할루시네이션(가짜 판례 등) 발견 사례가 2023년 10건 → 2025년 상반기 73건으로 급증
- AI 기업이 자체적으로 개선(RLHF)해도 "환각 세금(Hallucination Tax)" 같은 역효과가 있어,
  외부 검증 서비스가 구조적으로 필요하다는 논리로 사업을 설계함

## 핵심 기능 (도메인별 계층 검증 — 검증 "수단"이지 서비스 "정체성"이 아님)
- 일반 대화: 웹검색 기반 가벼운 교차검증 (무료)
- 법률: 대법원 판례검색 / 법제처 국가법령정보 공동활용 API 대조 (유료)
- 의료: 식약처 의약품안전나라, 국가건강정보포털 (유료)
- 금융: 금융감독원 전자공시(DART), 한국은행 경제통계시스템(ECOS) (유료)

## 지금까지 만든 MVP 프론트엔드 (yume_dashboard.jsx)
React 컴포넌트로 이미 다음이 구현되어 있음(단, 실제 API 연동 없이 데모 수준):
1. 중앙 로고 + "AI에게 질문하고 [로고] 로 확인하세요" 히어로
2. 텍스트 붙여넣기 → 클로드 API(web_search 포함)를 직접 호출해 실제로 사실 주장을 추출·검증
3. 결과 화면은 3탭 대시보드: ① 검증 결과 ② 근거 자료(출처 링크) ③ 관련 상품(쿠팡 검색 연결)
4. 왼쪽 오버레이 사이드바 — 검증 기록 히스토리 (현재는 브라우저 임시 저장소 수준, 실제 유저별 DB 아님)
5. 로그인/회원가입 UI는 **껍데기만 있고 실제 인증 로직 없음** — 반드시 새로 구현 필요

## 디자인 시스템
- 브랜드명: 유메(YUME), 일본어로 "꿈"이라는 뜻
- 로고: 연회색→검정 그라데이션 워드마크, 좌상단에 히라가나 "ゆ", 우하단에 "め"
  (별첨 이미지 파일 `yume_logo.png` 참고 — base64로 코드에 이미 심어져 있음)
- 전체 컬러: 파스텔 퍼플 톤, 몽환적인 분위기 (브랜드명 "꿈"과 어울리게)
  - 배경: 상단에서 은은하게 퍼지는 라벤더 그라데이션
  - 주요 액센트: 소프트 퍼플 그라데이션 (#B49AEE → #6B4FA8)
  - 검증 결과 색(초록/빨강/황토)은 기능적 의미가 있어 톤 유지, 배경만 파스텔화
- 히어로 중앙 로고 뒤에 은은한 퍼플 글로우(radial gradient blur) 효과 적용됨

## 다음 단계 (우선순위 순)
1. **실제 백엔드 구축**: 회원가입/로그인(세션 관리), DB(회원, 검증 기록 영구 저장)
2. **법률 도메인 실제 연동**: 법제처 국가법령정보 공동활용 Open API (open.law.go.kr)
   - 사용자가 OC(이용자 코드)를 발급받아야 함 — 무료, 회원가입 후 간단 신청
   - Secret Key 방식 아님(쿠팡과 다름), 상대적으로 안전하지만 서버에서 처리 권장
3. **쿠팡파트너스 실제 연동**: 사용자가 이미 파트너스 가입되어 있음
   - Access Key + Secret Key는 반드시 서버 환경변수로만 보관 (HMAC 서명 방식이라 클라이언트 노출 절대 금지)
   - 지금 프론트의 "관련 상품" 탭은 일반 쿠팡 검색 링크로만 되어 있어 실제 제휴 추적이 안 됨 → 교체 필요
4. **애니메이션 고도화 (중요, 디자인 지침 참고)**: Framer Motion 또는 GSAP 설치, 애플 사이트에서
   영상이 재생되는 듯한 수준의 역동적이고 몰입감 있는 인터랙션 적용
   - **단, 지금의 구조(레이아웃 틀)는 유지한다** — 히어로(중앙 로고+문장), 검증 카드,
     3탭 대시보드(검증결과/근거자료/관련상품), 왼쪽 오버레이 사이드바 구조는 그대로 두고
     그 위에서 전환·인터랙션·마이크로애니메이션의 완성도만 끌어올린다. 처음부터 새로 짜지 말 것.
   - 무료/유료 티어 차이(위 수익모델 참고)도 이 애니메이션 고도화 과정에서 시각적으로
     설계에 녹여낼 것 (예: 유료 전용 도메인 선택 시 잠금 해제되는 듯한 인터랙션 등)
5. **도메인 배포**: 사용자가 호스팅·도메인·DNS 포워딩을 직접 할 줄 앎, 빌드 결과물만 전달하면 됨

## 사업 관련 참고 (코딩과 별개, 알아두면 좋음)
- 사업 아이디어는 "모두의 창업 프로젝트"(중기부, 2차) 및 향후 "예비창업패키지"(2027년 공고 예정)
  지원을 염두에 두고 설계됨. 두 사업은 동시수행 불가 조건이 있음(공식 확인됨).
- 대표자: 정원영 (정보올림피아드 전국 2위, 해킹방어과 출신, 前 판다웨이브 대표)

## 2026-09-12 런칭 준비 업데이트 (최신 — 위 "MVP"·"다음 단계" 내용과 다르면 이쪽이 기준)

### 무엇이 바뀌었나
1. **특허 기술 적용** — 「검색공간 완전성 기반 부존재 신뢰도 정량화」(`특허명세서_부존재신뢰도정량화.docx`)를
   검증 엔진에 그대로 구현. 인용된 판례·법령·조문·DOI·arXiv·PMID·ISBN을 공식 자료에서 못 찾으면
   "없음"으로 단정하지 않고 `NEC = w1·C + w2·F + w3·(1−P)`로 판정한다.
   - 코드: `server/nec/` (identifiers=형식 검증부, searchSpace=검색공간·수록률 메타데이터, similarity=근접도,
     nec=산출·판정·안내, scholarly=학술 레지스트리 조회), `server/legalPipeline.js`, `server/identifierPipeline.js`
   - 가중치 법률 0.65/0.2/0.15, 학술 0.6/0.25/0.15 · 형식오류 임계 0.7(초과 시 탐색 생략, 청구항 2) · 판정 기준 0.7
   - 수록률(completeness)은 **추정값**이다(`server/nec/searchSpace.js`). 화면에도 "(추정)"으로 표시. 운영 데이터로 보정할 것.
   - 실측(법제처 라이브): 지어낸 `2019다999991` → 부존재 확실 0.748 / 한 글자 틀린 `2016다254476` → 확인 불가 + 실재 `2016다254467` 제시 /
     `1985헌마123` → 헌재 설립 전이라 탐색 없이 부존재 확실 / `주택임대차법` → `주택임대차보호법` 조문으로 대조
2. **DB** — **Supabase PostgreSQL**(`DATABASE_URL`). 로컬 개발·테스트는 `DATABASE_URL`이 없으면 PGlite(WASM Postgres)로 자동 전환.
   `server/db.js`에 스키마·마이그레이션. 테이블은 전용 스키마 `yume`(Supabase REST API로 노출되지 않음) + 전 테이블 RLS.
   회원·세션·검증 기록·주장·API 키·사용량·대화·오류·감사 로그·데이터 반출(파일 내용 포함)을 모두 DB에 저장 — 서버 디스크 불필요.
3. **로그인** — 이메일+비밀번호(scrypt 해시), httpOnly 쿠키 세션 30일, 로그인 실패 잠금, 비밀번호 재설정 메일(Resend),
   계정 설정(프로필·데이터 동의·API 키·비밀번호·내 데이터 내려받기·탈퇴). 탈퇴 시 기록 전부 즉시 파기.
4. **B2B API** — `/v1/verify`(동기 `wait` 또는 폴링), `/v1/usage`. 키는 계정에서 발급(원문은 한 번만 표시, DB엔 해시),
   월 한도·분당 한도, 요청 단위 과금 기록, 월별 청구 리포트(CSV). 문서: `/docs/api`.
5. **데이터셋 판매** — 동의한 회원 + 계약상 허용한 API 고객(키별 `data_sharing`) 데이터만, 주장 단위로 가명처리
   (원문·이메일·IP 제외, 이름·연락처·계좌 등 마스킹, 레코드 ID는 솔트 해시, 시각은 월 단위)해 JSONL/CSV로 반출.
   반출 링크는 만료·횟수 제한, 반출·다운로드는 감사 로그에 남는다. 익명·카카오 데이터는 절대 포함 안 됨.
6. **운영 대시보드** `/admin` — 개요(KPI·30일 차트), 회원(요금제·만료일·토큰·정지·권한), 검증 기록(원문·NEC 상세),
   API·과금, 데이터셋 판매, 대화, 오류, 감사 로그, DB 백업. `ADMIN_EMAILS`에 적힌 이메일 계정이 관리자.
7. **법적 페이지** — `/terms` 이용약관, `/privacy` 개인정보처리방침(국외 위탁: Anthropic·Supabase·Railway·Resend 명시),
   보유 기간은 서버 유지보수 작업이 실제로 집행(비회원 기록 180일, API 기록 1년 등).
8. **정리한 데모 요소** — 결제 없이 플랜을 바꾸던 버튼, 공짜 토큰 충전(웹·카카오 "토큰충전"), 누구나 발급하던
   데모 API 키(`POST /v1/keys`)를 모두 제거. 유료 플랜은 관리자가 대시보드에서 부여(요금제 창은 "이용 문의").

### 배포 체크리스트 (Supabase + Railway)
1. Supabase 프로젝트 → Connect → **Session pooler** 연결 문자열(5432)을 Railway 변수 `DATABASE_URL`에 넣는다.
   Transaction pooler(6543)는 쓰지 말 것. 볼륨은 필요 없다. Railway Replicas는 1(요청 제한이 인스턴스 메모리 기준).
2. `server/.env.example`의 값 채우기: `NODE_ENV=production`, `PUBLIC_BASE_URL`, `ADMIN_EMAILS`, `ADMIN_SECRET`, `DB_REGION_LABEL`,
   `DATASET_SALT`(한 번 정하면 변경 금지), `RESEND_API_KEY`·`MAIL_FROM`(도메인 인증 필요), 사업자 정보,
   `KAKAO_SKILL_SECRET`(카카오 오픈빌더 스킬 설정 헤더 `X-Yume-Skill-Token`에 같은 값 — 없으면 운영에서 스킬 거부).
3. 빌드 `npm run build` → 시작 `npm start`. Node 24(`package.json` engines로 고정). 첫 기동 때 테이블이 자동 생성된다.
4. 배포 후 `/api/health`에서 `db·persistentDb·claudeConfigured·lawApiConfigured·mailConfigured`가 모두 true인지 확인.
5. `ADMIN_EMAILS`의 이메일로 가입 → `/admin` 접속 확인. 백업은 Supabase(Database → Backups)가 자동, `/admin` DB 탭에서 수동 사본(NDJSON)도 받을 수 있다.
6. 테스트: `npm test`(단위·통합 31개, PGlite) / `TEST_DATABASE_URL=… npm test`(실제 Postgres) / `npm run test:legal`(법제처·Claude 실연동, 비용 발생).

### 런칭 전 사람이 직접 해야 할 것
- **변리사·법률 검토**: 개인정보처리방침·이용약관은 실제 데이터 처리에 맞춰 작성했지만 법률 검토를 받을 것.
  특히 데이터셋 제3자 제공은 "제공받는 자"를 계약 시 방침에 추가 공개해야 함.
- **결제(PG) 미연동**: 유료 플랜·토큰 판매는 PG 계약(토스페이먼츠 등)과 통신판매업 신고 후 연동 필요.
  그 전까지는 관리자 부여 방식. 결제를 붙일 때 플랜 변경·토큰 지급(`usageStore.grantTokens`)은 PG 결제 승인 확인 뒤에만 호출할 것.
- 사업자등록번호·주소·통신판매업 신고번호를 환경변수에 넣으면 약관·푸터에 표시된다.
- `docs/유메_API_개발_설명서.html/pdf`는 옛 데모 API 기준 — 최신 문서는 서비스의 `/docs/api`.

## 절대 하지 말아야 할 것
- 로그인 UI를 "그럴듯하게 보이지만 동작 안 함" 상태로 실제 서비스처럼 노출하지 말 것 (사용자 기만)
- 결제 없이 유료 플랜·토큰이 지급되는 버튼을 다시 넣지 말 것 (비용 누수 + 기만)
- 동의하지 않은 사용자·익명·카카오 데이터를 데이터셋에 넣지 말 것 (`server/dataset.js`의 ELIGIBLE_WHERE가 유일한 관문)
- 쿠팡/법제처 API의 Secret Key·OC코드를 프론트엔드 코드에 하드코딩하지 말 것
- "AI 서비스"라는 표현을 마케팅 카피에 쓰지 말 것 (포지셔닝 위반)