# 리머(REAMER) 사이트 프로젝트 인수인계 문서

이 문서는 Claude(claude.ai)와의 대화에서 결정된 내용을 요약한 것입니다.
작업을 시작하기 전에 이 문서를 먼저 읽어주세요.

## 회사 구조 (매우 중요 — 먼저 이해할 것)
- **리머(REAMER)** = 모회사. **AI 소프트웨어 회사**다 — "정밀공학"은 실제 업종이 아니라 "대충 뚫린 것을 정밀하게 다듬는다"는 브랜드 미션/은유일 뿐. 사이트 카피에서 "분야"는 정밀공학이 아니라 **AI 소프트웨어**로 표기할 것 (2026-09-09에 정정됨).
- **제품은 3개 체제** (2026-09-10 확정). "1호 제품 / 2호 제품" 식으로 순번을 매기지 말 것 — **세 개를 나란히 운영하는 체제**라는 게 보여야 함.
  - **유메(YUME)** — AI 답변 팩트체크. 운영 중. 실제 도메인: **https://www.yume-reamer.com/**
  - **아이픽(AIpick)** — 생성형 AI 기반 대화형 상품 추천. 개발 중. 상세 스펙은 아래 "아이픽(AIpick) 제품 스펙" 섹션 참고.
  - **AMM 프로토콜(Auto Make Money Protocol)** — 자동 매매 프로토콜. 개발 중. (2026-09-10에 "오토레이"에서 이 이름으로 확정 — 옛 이름은 쓰지 말 것)

이 문서는 **리머 회사 사이트** 작업용입니다. 유메 자체 서비스 코드가 아닙니다.

## 지금까지 만든 것 — 디자인 시스템 원본 (reamer_site.jsx)
React 아티팩트로 이미 리머 사이트의 1차 버전을 만들어뒀습니다. 이 파일이 **리머의 확정된 브랜드 아이덴티티 원본**이며, 앞으로 추가하는 모든 요소는 이 톤에 맞춰야 합니다.

### 확정된 디자인 토큰
- **컨셉**: 정밀공학 스튜디오. 블루프린트(설계도)·엔지니어링 카탈로그 느낌
- **배경**: 다크 그래파이트 블랙(#0D0D0D 계열)
- **텍스트**: 오프화이트(#F2F0EB)
- **포인트 컬러**: 구리/앰버 색(#B8763E) — 딱 하나만 절제해서 사용, 남발하지 말 것
- **타이포그래피**: 본문은 깔끔한 산세리프(-apple-system 등), 수치·라벨류는 모노스페이스(JetBrains Mono류) — 설계도 느낌을 위해 의도적으로 사용
- **로고**: 육각형(정밀공구의 hex shank에서 착안) 6개 꼭짓점에 R-E-A-M-E-R 각 글자 배치, 중앙에 4개(또는 6개) 날개의 회오리(터빈 블레이드) 모양이 회전. 로고 자체는 흑백 선도, 큰 사이즈에서만 글자 표시(작은 사이즈는 회오리+육각형만)
- **모션**: 히어로 섹션은 스크롤에 연동된 시네마틱 시퀀스(화면 고정 + 텍스트 3단계 크로스페이드 + 배경 회오리 회전/확대) — 순수 CSS/스크롤 위치 계산으로 구현됨(외부 애니메이션 라이브러리 없이)
- **유메 제품 카드 섹션만 예외**: 리머 톤(다크) 안에 유메 자체의 파스텔 퍼플 톤을 그대로 보여주는 카드를 넣어서 "차가운 엔지니어링 회사가 따뜻한 제품을 만든다"는 의도적 대비를 연출함 — 이 대비 장치는 유지할 것

## OriginKit 컴포넌트 사용 방침 (중요)
사용자가 OriginKit(컴포넌트 마켓플레이스)에서 섹션들(예: "Hero 19" 등)을 골라 CLI로 설치하면서 사이트를 조합할 예정입니다.

```
npx originkit@latest add hero-19 --prompt
```

같은 명령어를 **사용자 본인 로컬 환경에서 직접 실행**합니다. `ORIGINKIT_API_KEY`는 사용자의 실제 인증키이므로:
- **절대 코드에 하드코딩하지 말 것** — `.env` 파일에 넣고 `.gitignore`에 반드시 포함
- Claude(claude.ai)와의 대화에는 이 키가 노출된 적이 있어 사용자가 재발급을 고려 중일 수 있음 — 혹시 이전 키가 남아있다면 재발급 여부 확인 권장

**핵심 지침**: OriginKit에서 가져온 컴포넌트는 **기본 스타일 그대로 쓰지 말고, 위에 정리한 리머 디자인 토큰(다크+구리색 포인트+육각형/모노스페이스 감성)에 맞춰 색상·폰트·여백을 반드시 재조정**해야 합니다. OriginKit 컴포넌트는 레이아웃 구조(섹션 구성)만 참고하고, 톤앤매너는 리머 고유의 것을 유지하는 것이 원칙입니다.

## 사용자가 별도로 전달할 것들
- 유메 실제 도메인 링크(yume-reamer.com 등) — 받으면 "제품" 섹션의 유메 카드에 실제 링크 연결
- 아이픽 관련 정보와 새 도메인 — 받으면 "제품" 섹션 두 번째 카드로 추가(점선 박스 "준비 중" 상태를 실제 카드로 교체)
- 추가 디자인 레퍼런스 스크린샷(OriginKit 등에서 캡처) — 받으면 레이아웃 구조만 참고하고 색상은 리머 톤 유지

## 지금까지 완성된 섹션 구조 (reamer_site.jsx 기준)
1. 네비게이션 (로고 + 제품/회사소개/인사이트/문의 메뉴)
2. 히어로 (스크롤 연동 시네마틱 시퀀스)
3. 스펙 스트립 (설립·대표·1호제품·분야를 블루프린트 라벨처럼 나열)
4. 제품 섹션 (유메 카드 + "다음 프로젝트 준비 중" 점선 박스)
5. 회사 소개 (대표자 프로필 + 미션 선언문)
6. 인사이트 (리서치 노트 카드 3개 — 기존 시장조사·특허 보고서 내용을 콘텐츠 카드로 재활용 가능)
7. 문의 (CTA + 이메일)
8. 푸터

## 다음 단계
1. OriginKit 컴포넌트를 사용자가 로컬에서 설치
2. 설치된 컴포넌트를 위 디자인 토큰에 맞춰 리스타일링
3. 아이픽 정보 수신 시 제품 섹션 확장
4. 유메 실제 링크 연결
5. 최종 배포(Railway 등, 유메와 동일한 방식 권장)

## 2026-09-09 업데이트 — 색상 방침 변경 + 진행 상황
- **색상**: 구리/앰버 포인트 컬러(#B8763E)는 보류. 당분간 **블랙/화이트/그레이 톤만 사용**하기로 결정(사용자 지시). 유메 카드 섹션의 파스텔 퍼플 대비 장치는 그대로 유지.
- **hero-28** (OriginKit, 원래 데모명 "Zentra" — 블랙홀/파티클 디스크 비주얼)을 홈 화면 히어로로 설치·채택. 이미 순수 블랙/화이트라 색상 작업 불필요, 카피만 리머로 교체:
  - 내비: REAMER 워드마크(육각형 로고 SVG는 아직 없어서 임시 사각 마크) + 제품/회사소개/인사이트/문의
  - 헤드라인: "대충 뚫린 것을 정밀하게, 다시 뚫는다"
  - 스펙 스트립: 설립 2026 · 대표 정원영 · 1호 제품 YUME · 분야 정밀공학
  - 가짜 "Trusted by" 파트너 로고 마퀴(Logiqo 등)는 실제 파트너가 없어 제거함
- **hero-43**: CLI가 이 컴포넌트는 실제 소스 대신 AI 빌드 프롬프트(HERO-PROMPT.md)만 내려줌(Pro/Template 한정 컴포넌트로 추정). 그 스펙(WebGL 블룸 쉐이더 + 캔버스 커서 트레일)을 흑백톤으로 직접 구현해서 `src/components/reamer/MissionHero.jsx`로 홈 화면 바로 아래 섹션에 배치. 자체 내비는 hero-28과 중복되어 제거.
- 로컬 미리보기: `npm run dev:client` 실행 후 `http://localhost:5173/reamer.html` (기존 유메 서비스는 `index.html`/`main.jsx`로 그대로 분리되어 있음).
- 아직 미구현: 제품/회사소개/인사이트/문의/푸터 섹션 — 유메 실제 링크, 아이픽 정보 등 콘텐츠 수신 후 진행.

## 2026-09-10 업데이트 — 3제품 체제 · 특허 변경 · 개발 포트폴리오

**특허가 바뀌었다.** 이전 초안(「시점 인식 기반 AI 생성콘텐츠 사실검증」)은 새 초안으로 대체됨:
- **「검색공간 완전성 기반 부존재 신뢰도 정량화 방법 및 시스템」** (Quantifying Non-Existence Confidence Based on Search-Space Completeness), 2026년 9월 작성, 변리사 미검토 초안
- 핵심: AI가 없는 판례·문헌을 지어냈을 때, 데이터베이스에서 "못 찾음"을 "없음"으로 단정하지 않는다. 탐색 커버리지 C, 식별자 형식오류 지수 F, 유사항목 근접도 P를 가중 결합해 부존재 신뢰도 `NEC = w₁·C + w₂·F + w₃·(1 − P)`를 산출하고, 미탐색 영역까지 함께 제시.
- 청구항 12개 (방법 1~9 / 시스템 10~11 / 기록매체 12). 특허청 「컴퓨터 관련 발명 심사기준」 대응해 하드웨어 구성 명시.
- 사이트에서는 개발 포트폴리오 섹션의 **대표(featured) 카드**로 배치, 수식까지 노출.
- ※ 이전 「시점 인식 기반」 건도 별도로 출원하는지는 미확인 — 확인되면 포트폴리오에 두 번째 특허 카드로 추가할 것.

**"인사이트(작업실에서 온 노트)" 섹션 → "개발 포트폴리오" 섹션으로 교체.** 가짜 아티클 카드 3개를 걷어내고 실제 개발 이력을 카드로 채움:
1. (featured) 특허 — 검색공간 완전성 기반 부존재 신뢰도 정량화
2. 유메 검증 파이프라인 — React / Express / 법제처 Open API / Claude API
3. 판다 익스체인지(PAX) — BNB 체인 기반 AI 자동 소각 디플레이션 토큰, 인플레이션 헷지 자산 목표. https://www.paxpresale.com
4. 저스트로맨스 — 트레이딩 지표 프로젝트, **다윗 · 골리앗 · 노아** 3개 프로그램
5. 폐렴 엑스레이 판독 모델 — 흉부 X-ray 딥러닝 분류

**판다웨이브 (2026-09-10 확인 완료)** — 넥스트유니콘 기업 정보에서 확인:
- 설립 2024.11.19, 대표 정원영, 서울 영등포구 당산동, 1~10명, 엔젤/프리IPO 단계
- 사업: "대화형 인공지능을 기반으로 투자 포트폴리오를 형성해줍니다. 당신의 경제 비서와 경제적 자유를 이루세요"
- 제품 2종: **펀드메이트(Fund Mate)** — 대화형 AI 경제 포트폴리오 / **데일리메이트(Daily Mate)** — 일정 관리·습관 분석
- 업종: IT(소프트웨어/앱/웹, 플랫폼), 금융(개인자산관리), SNS/커뮤니티
- 사이트에는 펀드메이트·데일리메이트를 **각각 독립 포트폴리오 카드**로 넣었고, 연혁의 판다웨이브 항목도 2024년으로 채움
- ※ 이전 유메 인수인계 문서에는 "前 판다웨이브 대표"로 적혀 있었으나 넥스트유니콘에는 현직 대표로 등록되어 있음 — 사이트에는 "판다웨이브 설립 · 대표"로 중립 표기함. 前/현직 여부 확인 필요

**저스트로맨스 (2026-09-10 내용 수신)** — 트레이딩뷰 차트용 지표, **파인 스크립트로 직접 개발**, 트레이딩뷰 API 연동 경험 있음. 세 프로그램:
- **다윗** — 역추세 지표. 오더블록·FVG 등 스마트 머니 컨셉(SMC) 기반
- **골리앗** — 추세 지표. 이평선·매수강도 등 500가지 이상의 기법을 학습
- **노아** — 빅 쇼트(급락) 예상 지표. ICT 기반, 사용자가 미리 빠져나오게 하는 것이 목적
- ⚠️ **AMM 프로토콜과는 완전히 별개 프로젝트다.** 둘 다 트레이딩 도메인이라 이어진 것처럼 쓰기 쉬운데, 사용자가 명시적으로 무관하다고 못박음. 저스트로맨스 = 사람이 차트 보고 판단하는 **지표**, AMM = 알아서 실행하는 **자동 매매 프로토콜**. 카피에서 둘을 엮지 말 것.

**⚠️ 아직 확인 필요**
- **연도**: 판다웨이브(2024), 특허·리머 설립(2026) 외에는 전부 `20XX` 플레이스홀더 — 정보올림피아드, 한국디지털미디어고 졸업, 중앙대 재학, 폐렴 판독 모델, 판다 익스체인지, 저스트로맨스, 법정 분쟁 연도 필요

**줄바꿈 처리 (사이트 전역)**
- `word-break: keep-all` — 한글이 단어 중간에서 잘리지 않게. `.site`에 걸어 전역 상속.
- `.force-break` — 데스크톱용 의도적 2줄 분할 `<br>`을 640px 이하에서 숨김. 좁은 화면에서 강제 줄바꿈 + 자연 줄바꿈이 겹쳐 어중간하게 잘리던 문제 해결.
- 새 문장을 추가할 때도 이 두 가지를 전제로 쓸 것.

## 아이픽(AIpick) 제품 스펙 (2026-09-09 수신 — 리머 2호 제품, 별도 앱 개발 건)
> 이 스펙은 **리머 회사 사이트 작업과는 별개의 프로젝트**다. 아이픽은 대화형 AI 상품 추천 웹앱 자체를 처음부터 만드는 큰 작업(대화 엔진·상품 매칭·대시보드 UI·PWA)이라, 회사 사이트의 "제품" 카드에 링크 하나 넣는 것과는 스코프가 다르다. 사용자가 명시적으로 "아이픽 개발 시작해줘"라고 하기 전까지는 사이트에 아이픽 카드(설명 텍스트 + "개발 중" 표시)만 추가하고, 앱 자체를 새로 만들지 말 것.

**서비스 개요**: 사용자가 "뭘 사고 싶은지" 말하면 AI가 니즈를 구체화하는 질문을 이어가다가, 충분한 정보가 모이면 대화를 종료하고 상품 3개를 대시보드로 추천.

**핵심 의사결정**
- 상품 데이터: 쿠팡/네이버/지마켓 등 — 크롤링은 이용약관상 리스크 있음. **쿠팡파트너스 API / 네이버쇼핑 API 등 공식 제휴 API 우선 검토**. MVP는 목업 데이터로 개발, 실제 연동은 "TODO: 데이터 소스 최종 확정 필요"로 보류.
- MVP 플랫폼: 웹(반응형/PWA) 먼저 → 이후 네이티브 앱 확장
- 대화 종료: LLM 자체 판단 + 안전장치(질문 최대 7개 soft cap, 사용자가 "지금 추천받기" 버튼으로 강제 종료 가능, 정보 부족 시 재질문 없이 LLM이 추정)
- 상품 매칭: 별도 벡터 DB 없이, 대화 요약 → 카테고리/가격대 규칙 기반 1차 필터링 → 후보 10~30개를 프롬프트에 넣어 **LLM이 3개 직접 선정 + 추천 이유 생성**. 후보가 수백~수천 개로 늘면 임베딩 기반 사전 필터링을 향후 확장 과제로 남김.

**대화 엔진 설계**
- 한 턴에 질문 1개만. 우선순위: 카테고리 → 예산 → 용도/상황 → 스타일/브랜드 → 기타 제약(색상·사이즈 등)
- 사용자가 애매하게 답하거나 회피하면 재질문하지 않고 다음 주제로 이동
- 대화 히스토리 전체를 세션 상태로 누적, 종료 시 니즈 요약(카테고리/예산/용도/스타일/제약)을 별도 생성해 매칭 단계로 전달

**화면 구성 (MVP)**: ① 온보딩("무엇을 찾으시나요?" 자유 입력) ② 대화 화면(챗봇 UI + "지금 추천받기" 버튼 상시 노출) ③ 결과 대시보드(상품 3개 카드: 이미지·상품명·가격·추천 이유·구매 링크·"다시 찾기" 버튼)

**기술 스택 제안**: React 반응형 웹(PWA) / Node.js 또는 Python API 서버 / Anthropic Claude API(Messages API, 대화 히스토리 관리) / 상품 데이터는 목업 JSON→이후 제휴 API / 세션 DB는 SQLite·PostgreSQL 등 경량 DB

**개발 우선순위**: ① 대화 엔진(프롬프트+턴 관리+종료 판단) ② 목업 상품 데이터+매칭 로직 ③ 대화 UI+결과 대시보드 ④ PWA 설정 ⑤ (이후) 실제 상품 데이터 소스 연동(법률 검토 후)

## 2026-09-09 전면 재구축 (최신 — 이 항목이 현재 코드 기준)

이전에 만들었던 섹션별 구현(HeroScrollStage / MissionHero / FounderTimeline / BlackHoleCover)은 **전부 삭제**했습니다. 섹션마다 배경과 이펙트를 따로 갖고 있어서 이음매·팝·정지 구간이 생기는 게 근본 원인이었습니다.

**새 구조 — 배경은 사이트 전체에 딱 하나**
- [SiteBackdrop.jsx](src/components/reamer/SiteBackdrop.jsx): `position: fixed` 전체 화면 레이어 하나가 문서 전체의 배경을 담당. rAF 루프 하나에서 모션 상태(스크롤 진행도·포인터)를 갱신하고 세 레이어를 같은 프레임에 그림 → 레이어끼리 어긋나지 않음. **스크롤 중 React 리렌더 0회.**
  - [layers/bloom.js](src/components/reamer/layers/bloom.js): WebGL 빛 번짐. 사이트 전체가 같은 조명 아래 있게 됨(섹션마다 배경이 바뀌지 않음).
  - [layers/disc.js](src/components/reamer/layers/disc.js): 파티클 원반. **CSS scale을 절대 쓰지 않음** — 반지름 자체가 스크롤의 연속 함수이고 매 프레임 네이티브 해상도로 다시 그리므로 아무리 커져도 화질이 뭉개지지 않음. 파티클 반지름을 0~1 정규화로 저장하는 게 "팍 커짐" 없이 연속으로 커지는 핵심.
  - [layers/trail.js](src/components/reamer/layers/trail.js): 마우스 따라다니는 글자 타일 트레일. 뷰포트 고정 캔버스 하나라 **1·2·3 섹션 전부에서 동일하게 동작**. 텍스트는 `.trail-ch` span으로 쪼개두면 타일이 지나갈 때 글자가 어두워져 가독성 유지.
- 모든 섹션은 배경 투명. 콘텐츠가 이 하나의 장면 위를 지나갈 뿐이라 **섹션 경계라는 개념 자체가 없음.**

**스크롤 연출 3막** (정지 구간 없음 — 어느 위치에서도 계속 움직임)
1. 도입(p 0→0.42): 원반이 연속으로 커지며 화면을 삼킴, 조명이 광원으로 조여듦
2. 중반(p 0.42→0.72): 원반 테두리는 화면 밖으로 빠져나가고, 조명 반경이 극단적으로 넓어져 균일한 앰비언트로 전환 → 콘텐츠 가독성 확보. 파티클 20%는 "모트"로 남아 계속 흘러다님
3. 피날레(p 0.72→1): 원반이 다시 작아지며 멀어짐 — 통과한 뒤 뒤돌아보는 연출

**핀 고정(sticky) 제거**: 스크롤을 가로채는 구간이 없어서 어디서든 계속 스크롤됩니다.

**섹션 구성**: Hero → Manifesto → Products(유메 실링크 + 아이픽) → **소개(한국어, 대표 연혁)** → Insights → Contact → Footer. 소개 섹션만 한국어, 나머지는 영어.

**남은 확인 사항**
- 연혁 연도가 전부 `20XX` 플레이스홀더 — 실제 연도 필요
- 연결 도메인 확정: **www.d-reamer.com**. 연락처 이메일도 `hello@d-reamer.com`으로 맞춰둠 — 실제 수신 가능한 주소인지 확인 필요
- 배포: `dist-reamer/` 정적 빌드를 별도 Railway 서비스로 배포 (`npm run build:reamer` + `npm run start:reamer`, `serve` 패키지 사용). 유메 Express 서버(`server/index.js`)는 모든 경로를 `index.html`로 캐치올하므로 같은 서비스에 얹으면 안 됨 — 반드시 분리된 서비스
- Insights 카드 3개는 제목/요약만 있는 `Coming soon` 상태

**기술 메모**
- React.StrictMode는 여전히 `src/main-reamer.jsx`에서 빼둔 상태
- Tailwind/OriginKit 의존 제거 — 사이트는 순수 CSS([site.css](src/components/reamer/site.css))로 재작성됨. `src/components/originkit/**` 벤더 파일은 남아있지만 더 이상 import하지 않음

## (구) 2026-09-09 업데이트 — 스크롤 줌 히어로 + 대표 연혁 섹션 — 위 재구축으로 대체됨
- **히어로 스크롤 연동 효과** ([HeroScrollStage.jsx](src/components/reamer/HeroScrollStage.jsx), 1280px 이상 데스크톱에서만 활성 — 모바일/태블릿은 hero-28 콘텐츠가 100vh보다 커서 핀 고정 시 잘리므로 일반 스크롤 유지): 스크롤 내리면 nav/텍스트 → 작은 블랙홀 디스크 순으로 페이드아웃되면서, 별도로 렌더링한 풀스크린 블랙홀([BlackHoleCover.jsx](src/components/reamer/BlackHoleCover.jsx))이 크로스페이드로 화면을 덮고, 그대로 미션 섹션으로 스크롤 다운. **중요 — 절대 CSS `transform: scale()`로 이 디스크를 키우지 말 것**: 캔버스 파티클을 래스터로 늘리면 화질이 깨진다. 대신 별도 인스턴스를 처음부터 목표 크기(풀스크린)로 렌더링해 두고 opacity만 크로스페이드하는 방식으로 우회함.
- **⚠️ React.StrictMode가 Motion(framer-motion 후속) 라이브러리의 `useScroll`/`useMotionValueEvent` 스크롤 추적을 깨뜨림** — StrictMode의 effect 이중 실행 때문에 스크롤 리스너가 죽어서 값이 전혀 갱신되지 않았음. `src/main-reamer.jsx`에서 StrictMode를 뺀 것으로 해결(유메 본체 `main.jsx`는 손대지 않음, 리머 사이트 한정 이슈). 앞으로 이 사이트에 Motion 스크롤 훅을 더 쓸 일이 있으면 StrictMode를 다시 넣지 말 것.
- **`useTransform`을 motion.div의 `style`에 직접 바인딩하는 방식도 불안정**했음(빠른/순간 스크롤 시 한 틱 뒤처져서 "점점"이 아니라 "갑자기" 커지는 것처럼 보임) → `useMotionValueEvent(scrollYProgress, "change", ...)`로 매 틱마다 순수 함수로 재계산한 값을 React state에 넣어 plain style로 적용하는 방식으로 교체해 해결. 이 사이트에서 스크롤 연동 애니메이션을 더 추가할 때는 이 패턴을 재사용할 것.
- **대표 연혁 섹션** ([FounderTimeline.jsx](src/components/reamer/FounderTimeline.jsx)): 유메 인수인계 문서의 실제 정보(정보올림피아드 전국 2위, 해킹방어과, 前 판다웨이브 대표, 법정 분쟁 중 AI 오답 피해 경험 → 유메 창업 계기, 2026 리머 설립, 아이픽 개발 중)를 스크롤 연동 타임라인으로 구현. **연도는 전부 "20XX" 플레이스홀더** — 정확한 연도를 사용자에게 받아서 채워야 함.
- **섹션 배경 통일**: hero-28 꼬리(블랙홀 커버) → 미션 섹션 → 대표 연혁 섹션이 이어지도록 미션 섹션 하단과 연혁 섹션 상단에 옅은 회색 그라데이션을 겹쳐 이음매를 부드럽게 만듦.
- 유메 실제 도메인(https://www.yume-reamer.com/)과 아이픽 정보를 붙일 "제품" 섹션은 아직 미구현.

## 절대 하지 말아야 할 것
- OriginKit 컴포넌트의 기본 라이트톤/기본 컬러를 그대로 두지 말 것 — 반드시 리머 다크+구리색 톤으로 재작업
- API 키를 코드에 직접 쓰거나 커밋하지 말 것
- 유메의 파스텔 퍼플 톤을 리머 사이트 전체에 확산시키지 말 것 — 유메 카드 안에서만 의도적으로 사용되는 대비 장치임
